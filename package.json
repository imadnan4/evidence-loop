{
  "name": "evidence-loop",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.0.0"
  },
  "scripts": {
    "check": "pnpm --filter @evidence-loop/contracts check",
    "check:contracts": "pnpm --filter @evidence-loop/contracts check"
  }
}
